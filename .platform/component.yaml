apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "testeste-templrate-spec-final"
  description: "Artifacts generator via Spec."
  annotations:
    github.com/project-slug: sjflausino/testeste-templrate-spec-final
    backstage.io/techdocs-ref: dir:..
    github.com/workflows: publish.yml,remove.yml
    kong-manager/service-name: app.testeste-templrate-spec-final.80
    kong-manager/instance: /kong-manager/
spec:
  type: devops
  lifecycle: experimental
  owner: "group:default/admin"
  providesApis:
    - testeste-templrate-spec-final
